// TEMPORARY verifier probe — a deliberate vue-tsc type error, to prove the
// Frontend build gate actually fails on bad code. Removed in the next commit.
const probe: number = 'this is not a number'
export default probe
