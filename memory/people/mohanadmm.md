---
type: person
role: مطوّر
github: mohanadmm
---

# mohanadmm

متعاون على المستودع.

## ⚠️ لازم يعمل ده قبل أي pull

التاريخ اتكتب من تاني أكتر من مرة (force push):

```bash
git fetch origin && git reset --hard origin/main
```

`git pull` العادي هيديله تعارضات.

## حاجات يعرفها

- فيه `PreToolUse` hook في `.claude/settings.json` بيمنع التعديل على
  المستندات التلاتة المرجعية. مقصود، بس هو ما اتفقش عليه.
- الملفات العربية في `arabic/` **للقراءة بس** (`D-58`).

## سجل التواصل

